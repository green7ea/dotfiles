{
  "reload_style_on_change": true,
  "layer": "top",

  // Choose the order of the modules
  "modules-left": ["tray", "battery", "network"],
  "modules-center": ["niri/workspaces"],
  "modules-right": ["pulseaudio", "backlight", "temperature", "clock"],

  "tray": {
    "icon-size": 21,
    "spacing": 10
  },

  "clock": {
    "format": "{:%a %b %d     %H:%M}",
    "tooltip": false,
    "on-click": "gnome-calendar"
  },

  "temperature": {
    "critical-threshold": 55,
    "format": "{temperatureC}°C",
    "on-click": "ghostty -e btop"
  },

  "backlight": {
    "format": "{percent}% {icon}",
    "format-icons": ["⦿", "⊜", ""]
  },

  "battery": {
    "states": {
      "full": 85,
      "good": 60,
      "warning": 30,
      "critical": 15
    },
    "format": "",
    "format-discharging": "{time} {icon} {power:>1.0f}W",
    "format-charging": "{capacity}% ",
    "format-alt": "{capacity}% {time} {icon}",
    "format-icons": ["", "", "", "", ""],
    "tooltip-format-discharging": "{power:>1.0f}W↓ {capacity}%",
    "tooltip-format-charging": "{power:>1.0f}W↑ {capacity}%"
  },

  "network": {
    "format-wifi": "{essid} ({frequency} GHz)  {signalStrength}%",
    "format-ethernet": "{ifname} ",
    "format-linked": "{ifname} (No IP) ",
    "format-disconnected": "Disconnected ⚠",
    "on-click": "cosmic-settings network"
  },

  "pulseaudio": {
    //"scroll-step": 1,
    "format": "{volume}% {icon}",
    "format-bluetooth": "{volume}% {icon}",
    "format-muted": "",
    "format-icons": {
      "headphones": "",
      "handsfree": "",
      "headset": "",
      "phone": "",
      "portable": "",
      "car": "",
      "default": ["", ""]
    },
    "on-click": "ghostty -e bluetui"
  }
}
